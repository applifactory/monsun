.button, button
  display: inline-block
  background: $gray
  color: white
  padding: 0.5rem 1.2rem 0.6rem
  margin: 0 0.1em
  cursor: pointer
  font-size: 1.2rem
  border: none
  &:hover
    background: lighten($gray, 20%)
    color: white
